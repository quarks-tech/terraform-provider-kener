---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "kener_monitor Resource - kener"
subcategory: ""
description: |-
  Manages a Kener monitor (an uptime/health check). Monitors are identified by their immutable tag.
---

# kener_monitor (Resource)

Manages a Kener monitor (an uptime/health check). Monitors are identified by their immutable `tag`.

## Example Usage

```terraform
# A simple HTTP/API monitor.
resource "kener_monitor" "api" {
  tag          = "my-api"
  name         = "My API"
  description  = "Health endpoint for the public API"
  monitor_type = "API"
  cron         = "* * * * *"

  # type_data is a JSON object whose fields depend on monitor_type.
  type_data = jsonencode({
    url     = "https://api.example.com/health"
    method  = "GET"
    timeout = 10000
  })
}

# A TCP monitor with several hosts.
resource "kener_monitor" "tcp" {
  tag          = "db-tcp"
  name         = "Database TCP"
  monitor_type = "TCP"
  is_hidden    = true

  type_data = jsonencode({
    hosts = [
      { type = "IP4", host = "db1.internal", port = 5432, timeout = 1000 },
      { type = "IP4", host = "db2.internal", port = 5432, timeout = 1000 },
    ]
  })
}

# A group monitor that aggregates the two monitors above.
resource "kener_monitor" "group" {
  tag          = "platform"
  name         = "Platform"
  monitor_type = "GROUP"

  type_data = jsonencode({
    latencyCalculation = "AVG"
    executionDelay     = 1000
    monitors = [
      { tag = kener_monitor.api.tag, weight = 0.5 },
      { tag = kener_monitor.tcp.tag, weight = 0.5 },
    ]
  })
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Human-readable monitor name.
- `tag` (String) Immutable, URL-friendly slug that uniquely identifies the monitor. Changing it forces recreation. Must match `^[a-z0-9]([a-z0-9_-]*[a-z0-9])?$`.

### Optional

- `category_name` (String) Optional category used to group monitors on the status page.
- `confirmation_threshold` (Number) Number of consecutive checks (1–60) required before a status change is recorded. Defaults to `1`.
- `cron` (String) Cron expression controlling how often the monitor runs (e.g. `* * * * *`).
- `default_status` (String) Status shown when there is no realtime data. One of `UP`, `DOWN`, `DEGRADED`, `MAINTENANCE`, `NO_DATA`. Defaults to `UP`.
- `description` (String) Optional description shown on the status page.
- `external_url` (String) Optional external link associated with the monitor.
- `image` (String) Optional logo/icon URL for the monitor.
- `include_degraded_in_downtime` (Boolean) Whether DEGRADED status counts against uptime. Defaults to `false`.
- `is_hidden` (Boolean) Whether the monitor is hidden from the status page. Defaults to `false`.
- `monitor_settings_json` (String) Free-form additional monitor settings as a JSON object.
- `monitor_type` (String) Kind of check. One of `API`, `PING`, `TCP`, `DNS`, `NONE`, `GROUP`, `SSL`, `SQL`, `HEARTBEAT`, `GAMEDIG`, `GRPC`. Defaults to `API`.
- `status` (String) Whether the monitor is enabled. One of `ACTIVE`, `INACTIVE`. Defaults to `ACTIVE`.
- `type_data` (String) Type-specific configuration as a JSON object, e.g. `jsonencode({ url = "https://example.com" })`. The exact fields depend on `monitor_type` (see the Kener monitor docs). Kener merges server-side defaults into this value; the provider stores the configured value verbatim.

### Read-Only

- `id` (String) Internal numeric identifier assigned by Kener.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Monitors are imported by their tag.
# Note: type_data and monitor_settings_json are not recovered on import
# (Kener merges server-side defaults), so set them in your configuration after
# importing.
terraform import kener_monitor.api my-api
```
