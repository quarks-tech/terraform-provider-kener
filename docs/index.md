---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "kener Provider"
description: |-
  The Kener provider manages resources on a Kener https://kener.ing status-page instance via its /api/v4 REST API.
---

# kener Provider

The Kener provider manages resources on a [Kener](https://kener.ing) status-page instance via its `/api/v4` REST API.

## Example Usage

```terraform
terraform {
  required_providers {
    kener = {
      source = "quarks-tech/kener"
    }
  }
}

# Credentials can also be supplied via the KENER_ENDPOINT and KENER_API_TOKEN
# environment variables instead of these attributes.
provider "kener" {
  endpoint  = "https://status.example.com"
  api_token = var.kener_api_token # kener_... created in Settings -> API Keys
}

variable "kener_api_token" {
  type      = string
  sensitive = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `api_token` (String, Sensitive) Kener API token (`kener_...`), created in the Kener admin UI under Settings → API Keys. May also be set via the `KENER_API_TOKEN` environment variable.
- `endpoint` (String) Base URL of the Kener instance, including scheme and host (e.g. `https://status.example.com`). The `/api/v4` path is appended automatically. May also be set via the `KENER_ENDPOINT` environment variable.
